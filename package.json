{
  "name": "avenco-qa",
  "version": "0.0.0",
  "private": true,
  "description": "Infraestrutura de testes do Avenco. Não é empacotamento do app: o produto continua sendo servido como arquivos estáticos da raiz.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test \"tests/unit/**/*.test.mjs\"",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
